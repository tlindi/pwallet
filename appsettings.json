{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        "DefaultConnection": "Data Source=SimpLN.db"
    },
    "PhoenixBackend": {

        // Url for phoenixd server, example http://192.168.1.100:9740
        "ApiUrl": "SET BY USER",

        // Api password for phoenixd server
        "ApiPassword": "SET BY USER",

        // UiDomain relates to the domain used to reach the UI, example https://pwallet.app (no trailing /)
        "UiDomain": "SET BY USER",

        // LnUrlpDomain relates to the domain used for lnurl-p address
        // both URL's (UiDomain and LnUrlpDomain) needs to point to the application and can be same.
        // Example https://lnurl.pwallet.app (no trailing /) or https://pwallet.app
        // LnUrlDomain is not in use yet but will be!
        "LnUrlpDomain": "SET BY USER"
    }
}
